/** Arithmetic Operator */

//Addition +
/* let firstNumber = parseInt(window.prompt("Enter the first value"));
let secondNumber = parseInt(window.prompt("Enter the second value"));

let sum  = firstNumber + secondNumber;

window.alert(`The addition of ${firstNumber} and ${secondNumber} is ${sum}`) */

// Subtraction -
/* let firstNumber = parseInt(window.prompt("Enter the first value"))
let secondNumber = parseInt(window.prompt("Enter the second value"))

let sum = firstNumber - secondNumber;

window.alert(`The subtraction of ${secondNumber} from ${firstNumber} is ${sum}`) */

//Multiplication
/*let firstNumber = parseInt(window.prompt("Enter the first value"))
let secondNumber = parseInt(window.prompt("Enter the second value"))

let sum = firstNumber * secondNumber

window.alert(`The multiplication of ${firstNumber} and ${secondNumber} is ${sum}`)*/

//Division
/* let firstNumber = parseInt(window.prompt("Enter the first value"))
let secondNumber = parseInt(window.prompt("Enter the second value"))

let sum =  firstNumber / secondNumber

window.alert(`The division of ${firstNumber} and ${secondNumber} is ${sum}`)  */

// Modulus %

let firstNumber = parseInt(window.prompt("Enter the first number"));
let secondNumber = parseInt(window.prompt("Enter the second number"));

let sum  = firstNumber % secondNumber

window.alert(`The remainder of the division between ${firstNumber} and ${secondNumber} is ${sum}`)



